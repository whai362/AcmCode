#include <iostream>
#include <cstdio>
using namespace std;
bool G[N][N];
bool dp[N][N];
int main() {
	int n;
	while(scanf("%d", &n) != EOF) {
		memset(G, 0, sizeof(G));
		memset(dp, 0, sizeof(dp));
		for(int i = 0; i < n; ++i) {
			int u, v;
			sca
		}
	}
}
